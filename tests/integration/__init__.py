# Integration tests — test how components work together.
# These tests may require a database or external service (e.g. a test DB).
# They are slower than unit tests and are run separately in CI when needed.
